"use client";

export const revalidate = 0;
import CommonHeader from "../_component/common_header";

const AlbumUI = ({ }) => {

    return (
        <>
            <CommonHeader />
        </>
    );
}
export default AlbumUI;